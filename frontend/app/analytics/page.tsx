import { MetricCard, PageHeader, money } from "@/components/UI";
import { SimpleBar, TrendChart } from "@/components/Charts";
import { api } from "@/lib/api";
type Analytics={summary:{detected:number;above1:number;above2:number;above3:number;medianDuration:number;medianLiquidity:number;paperProfit:number};daily:Record<string,unknown>[];edgeDistribution:Record<string,unknown>[];durations:Record<string,unknown>[];categories:{name:string;value:number}[];profitBySize:Record<string,unknown>[]};
const empty:Analytics={summary:{detected:0,above1:0,above2:0,above3:0,medianDuration:0,medianLiquidity:0,paperProfit:0},daily:[],edgeDistribution:[],durations:[],categories:[],profitBySize:[]};
export default async function Performance(){let a=empty;try{a=await api<Analytics>('/analytics')}catch{}const charts=a.daily.length+a.edgeDistribution.length+a.durations.length+a.profitBySize.length>0;return <><PageHeader eyebrow="FORWARD TEST RESULTS" title="表现" english="Performance" description="真实市场数据与模拟执行结果。没有数据时不显示假图。"/><div className="mb-3 text-sm font-semibold text-zinc-500">时间范围 Time Range · 24H　7D　30D　ALL</div><section className="mb-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="检测机会" english="Detected Opportunities" value={a.summary.detected}/><MetricCard label="净套利空间大于 1%" english="Net Edge > 1%" value={a.summary.above1} detail={`> 2%: ${a.summary.above2} · > 3%: ${a.summary.above3}`}/><MetricCard label="中位持续时间" english="Median Duration" value={a.summary.detected?`${a.summary.medianDuration}s`:'—'} detail={`中位流动性 ${money(a.summary.medianLiquidity)}`}/><MetricCard label="已实现模拟盈亏" english="Realized Paper PnL" value={money(a.summary.paperProfit)} tone={a.summary.paperProfit>0?'positive':undefined}/></section>{charts?<section className="grid gap-5 xl:grid-cols-2"><Chart title="权益曲线" english="Equity Curve"><TrendChart data={a.daily}/></Chart><Chart title="优势分布" english="Edge Distribution"><SimpleBar data={a.edgeDistribution}/></Chart><Chart title="机会持续时间" english="Opportunity Duration"><SimpleBar data={a.durations}/></Chart><Chart title="利润与规模" english="Profit vs Trade Size"><SimpleBar data={a.profitBySize} x="size" y="profit"/></Chart></section>:<div className="card px-6 py-16 text-center"><h2 className="m-0 text-xl font-semibold">尚无足够的 Forward Test 数据</h2><p className="mt-2 text-sm text-zinc-500">No fabricated charts. 数据收集后自动显示真实表现。</p></div>}</>}
function Chart({title,english,children}:{title:string;english:string;children:React.ReactNode}){return <div className="card p-6"><h3 className="m-0 text-lg font-semibold">{title}</h3><p className="mb-5 mt-1 text-xs uppercase tracking-wider text-zinc-400">{english}</p>{children}</div>}
