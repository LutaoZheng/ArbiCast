"use client";
import { useState } from "react";
import { browserApi } from "@/lib/browser-api";
export function DynamicTestPanel(){const [data,setData]=useState<Record<string,any>|null>(null),[busy,setBusy]=useState(false);const run=async()=>{setBusy(true);try{setData(await browserApi("/research-test/dynamic",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({leader_venue:"polymarket",leader_before:.44,leader_after:.52,follower_before:.45,follower_after:.46,outcome:"HOME",size:25})}))}finally{setBusy(false)}};return <section className="card mt-5 p-6"><div className="mb-5 flex items-center gap-3"><span className="grid h-8 w-8 place-items-center rounded-full bg-black text-xs font-bold text-white">7</span><div><h2 className="m-0 text-xl font-semibold">动态信号行为测试</h2><div className="text-[10px] font-semibold uppercase tracking-wider text-zinc-400">Synthetic Dynamic Timing Test</div></div></div><p className="text-sm text-zinc-500">使用 production DynamicSignalEngine 验证 lead move、TTL 和 100–3000ms 多延迟执行。Synthetic 数据不写数据库，不进入 Research Performance。</p><button onClick={run} disabled={busy} className="rounded-xl bg-black px-4 py-2 text-sm font-semibold text-white disabled:opacity-40">{busy?"运行中…":"运行隔离测试 Run Isolated Test"}</button>{data&&<div className="mt-5"><div className="grid gap-3 sm:grid-cols-3"><Card label="Signal" value={data.signal?"CREATED":"NOT CREATED"}/><Card label="TTL Expiration" value={data.ttl_expired?"PASS":"FAIL"}/><Card label="Persistence" value={data.persisted?"ERROR":"NOT PERSISTED"}/></div><pre className="mt-4 max-h-80 overflow-auto rounded-2xl bg-zinc-950 p-4 text-xs text-zinc-200">{JSON.stringify(data.scenarios,null,2)}</pre></div>}</section>}
function Card({label,value}:{label:string;value:string}){return <div className="rounded-xl bg-zinc-50 p-4"><div className="label">{label}</div><b>{value}</b></div>}
